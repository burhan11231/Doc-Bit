import { Bell,LayoutDashboard,Menu,Settings,User } from 'lucide-react';import { Link,NavLink,Outlet } from 'react-router-dom';import { Button } from '@/components/ui/Button';import { useUiStore } from '@/stores/ui';import { authService } from '@/services/auth';
const nav=[['/dashboard','Overview',LayoutDashboard],['/settings/profile','Settings',Settings]] as const;
export function DashboardShell(){const toggle=useUiStore((state)=>state.toggleSidebar);return <div className="min-h-screen lg:grid lg:grid-cols-[280px_1fr]"><aside className="hidden border-r border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900 lg:block"><Link to="/dashboard" className="text-2xl font-black text-slate-950 dark:text-white">DocBit</Link><nav className="mt-8 space-y-2">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive}:{isActive:boolean})=>`flex items-center gap-3 rounded-xl px-3 py-2 ${isActive?'bg-brand-50 text-brand-700 dark:bg-slate-800':'text-slate-600 dark:text-slate-300'}`}><Icon size={18}/>{label}</NavLink>)}</nav></aside><main><header className="sticky top-0 z-10 flex h-16 items-center justify-between border-b border-slate-200 bg-white/90 px-4 backdrop-blur dark:border-slate-800 dark:bg-slate-950/90"><button className="lg:hidden" onClick={toggle}><Menu/></button><div className="font-semibold">Workspace</div><div className="flex items-center gap-3"><Bell size={20}/><User size={20}/><Button onClick={()=>void authService.signOut()} className="px-3 py-1.5">Sign out</Button></div></header><div className="p-6"><Outlet/></div></main></div>}
