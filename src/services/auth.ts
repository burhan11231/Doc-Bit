import { supabase } from '@/lib/supabase';
export const authService={signUp:(email:string,password:string,fullName:string)=>supabase.auth.signUp({email,password,options:{data:{full_name:fullName},emailRedirectTo:`${location.origin}/auth/verify`}}),signIn:(email:string,password:string,remember:boolean)=>supabase.auth.signInWithPassword({email,password}).then((result:unknown)=>{if(!remember)sessionStorage.setItem('docbit.sessionOnly','true');return result}),signInWithGoogle:()=>supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo:`${location.origin}/dashboard`}}),resetPassword:(email:string)=>supabase.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/auth/reset-password`}),updatePassword:(password:string)=>supabase.auth.updateUser({password}),signOut:()=>supabase.auth.signOut()};
