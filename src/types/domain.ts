export type AppRole='owner'|'admin'|'manager'|'editor'|'commenter'|'viewer';export type Theme='light'|'dark'|'system';export interface Profile{id:string;email:string;fullName:string|null;avatarUrl:string|null;role:AppRole;onboardingCompleted:boolean}export interface Workspace{id:string;organizationId:string;name:string;slug:string}export interface SessionUser{id:string;email?:string}export interface AuthSession{access_token?:string;refresh_token?:string;expires_at?:number;user?:SessionUser}
